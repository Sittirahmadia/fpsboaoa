package com.fpsboost.module.visual;
import com.fpsboost.module.Module;
public class LowFire extends Module {
    public LowFire() { super("low_fire", "Low Fire", "Lowers fire overlay so it doesn't block view", Category.VISUAL, false); }
}
